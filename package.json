{
  "name": "dfds-extension",
  "version": "1.0.0",
  "description": "Deepfake Detection System Extension",
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.jsx --bundle --outfile=app.js --loader:.jsx=jsx"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
